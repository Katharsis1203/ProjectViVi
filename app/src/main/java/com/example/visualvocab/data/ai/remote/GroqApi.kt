package com.example.visualvocab.data.ai.remote

// this interface is like a plan for how we talk to the Groq AI.
// we don't actually use it yet because we use OkHttp directly, but it's good to have it here just in case.
interface GroqApi {
    // contract for Groq API if needed
}
